package com.example.servivet.utils

import android.widget.RatingBar
import androidx.databinding.BindingAdapter

object BindingAdapters {

   /* @BindingAdapter("app:ratingAttrChanged")
    fun setRatingChangeListener(ratingBar: RatingBar, listener: RatingBar.OnRatingBarChangeListener?) {
        if (listener != null) {
            ratingBar.onRatingBarChangeListener = listener
        }
    }*/
}